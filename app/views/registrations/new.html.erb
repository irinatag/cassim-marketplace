<!-- SIMPLE FORMS - NOT WORKING RIGHT NOW! -->
<!-- <div class="well col-md-6 col-md-offset-2">
<h1>Sign Up for Distribu.td!</h1>
<%= simple_form_for @user, url: signup_path, html: {class: "form-group"} do |f| %>

<%= f.input :email, class: "form-control" %>
<%= f.input :password, class: "form-control" %>
<%= f.input :password_confirmation, class: "form-control" %>
<%= f.button :submit, "Sign Up", class: "btn btn-primary" %>

<% end %>
</div> -->


  <div class="col-sm-offset-3">
    <div class="col-sm-8">
      <div class="well">

          <h1>Sign Up</h1>

        <%= form_for @user, url: signup_path, html: {class: "form-horizontal"} do |f| %>

        <% if @user.errors.present? %>
        <ul>
          <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
          <% end %>
        </ul>
        <% end %>

        <div id="padding-form">
          <div class="form-group">
            <%= f.label :email, class: "control-label" %>
          </div>
        </div>


        <div id="padding-form">
          <div class="form-group">
            <%= f.email_field :email, class: "form-control" %>
          </div>
        </div>

        <div id="padding-form">
          <div class="form-group">
            <%= f.label :password, class: "control-label" %>
          </div>
        </div>


        <div id="padding-form">
          <div class="form-group">
            <%= f.password_field :password, class: "form-control" %>
          </div>
        </div>

        <div id="padding-form">
          <div class="form-group">
            <%= f.label :password_confirmation, class: "control-label" %>
          </div>
        </div>

        <div id="padding-form">
          <div class="form-group">
            <%= f.password_field :password_confirmation, class: "form-control" %>
          </div>
        </div>

        <div class="form-group">
          <%= f.submit "Sign up", class: "btn btn-large btn-primary" %>
          <%= link_to ".. Sign In if already registered", signin_path %>
        </div>

        <% end %>
      </div>
    </div>
  </div>
