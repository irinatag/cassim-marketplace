<div class="container">
  <%= link_to 'Go to my Admin Dashboard Panel', admin_path(@vendor.id), class: "btn btn-success" %>
  <div>

    <div class="container jumbotron show_panel voffset">
      <div class="page-header">
        <h3 class="product-title"><%= @product.name %>
        </h3>
      </div>

      <div class=" image_panel col-sm-6">
        <div>
          <% if @product.etsy_image_url != "" && @product.etsy_image_url != nil %>
          <%= image_tag @product.etsy_image_url, class: "img-rounded img-responsive", alt: "image" %>
          <% else %>
          <%= image_tag @product.image, class: "img-rounded img-responsive", alt: "image" %>
          <% end %>
        </div>
        <div>
          <h4>Sold by: <%= link_to @product.vendor.name, vendor_path(@product.vendor) %>

            <% if @product.vendor.city != '' && @product.vendor.city != nil %>
            <i class='glyphicon glyphicon-map-marker'></i>
            <%= @product.vendor.city %> ,
            <% end %>
            <% if @product.vendor.state != '' && @product.vendor.state != nil %>
            <%= @product.vendor.state %>
            <% end %>
          </h4>
        </div>
      </div>

      <div class="container col-sm-6">
        <div class="product-desc"><%= @product.description %></div>
        <hr>
        <div class="product-price list-inline">
          <h4 class="text-right">Unit Price: <span style="color: green"><%= number_to_currency @product.price %></span></h4>
          <h6 class="text-right">Minimum Order: <span style="color: light-grey"><%= @product.moq %> units</span></h6>
        </div>

        <div class="product-info">
          <% if @product.brand != "" && @product.brand != nil %>
          <label>Brand:</label> <%= @product.brand %> <br>
          <% end %>
          <% if @product.sku != "" && @product.sku != nil %>
          <label>SKU:</label> <%= @product.sku %> <br>
          <% end %>
          <% if @product.msrp != "" && @product.msrp != nil %>
          <label>MSRP:</label> <%= @product.msrp %> <br>
          <% end %>
          <% if @product.category1 != "" && @product.category2 != "" && @product.category1 != nil && @product.category2 != nil %>
          <label>Categories:</label>
          <span class="list-inline">
            <li><%= @product.category1 %></li>
            <li><%= @product.category2 %></li>
          </span> <br>
          <% end %>
          <% if @product.upc != nil %>
          <label>UPC:</label> <%= @product.upc %> <br>
          <% end %>
          <% if @product.restrictions != "" && @product.restrictions != nil %>
          <label>Restrictions:</label> <%= @product.restrictions %> <br>
          <% end %>
          <% if @product.warehouse_zip != "" && @product.warehouse_zip != nil %>
          <label>Warehouse Zipcode:</label> <%= @product.warehouse_zip %> <br>
          <% end %>
          <% if @product.warranty != "" && @product.warranty != nil %>
          <label>Warranty:</label> <%= @product.warranty %> <br>
          <% end %>
          <% if @product.link1_title != "" && @product.link1_title != nil && @product.link1 != "" && @product.link1 != nil %>
          <%= link_to @product.link1_title, @product.link1 %> <br>
          <% end %>
          <% if @product.link2_title != "" && @product.link2_title != nil && @product.link2 != "" && @product.link2 != nil %>
          <%= link_to @product.link2_title, @product.link2 %> <br>
          <% end %>
        </div>

        <div class="product-stock">Available <%= @product.when_ready %>
          <span id="buy-button">
            <%= link_to "Add To Cart", buy_path, {class: "btn btn-primary btn-lg", id:"buy"}%>
          </span>
        </div>

      </div>
    </div>
  </div>
